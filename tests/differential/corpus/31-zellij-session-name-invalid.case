CASE_DESCRIPTION="an invalid --session name is rejected before any runtime argument is built"
CASE_EXPECT_RUNTIME_ARGS=0

case_args() {
  CASE_ARGS_OUT=(--zellij --session "bad/name" -N -s -C "$1")
}

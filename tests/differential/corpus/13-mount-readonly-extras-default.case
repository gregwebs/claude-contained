CASE_DESCRIPTION="--readonly-extras flips the default for an unsuffixed extra mount"

case_setup() {
  mkdir -p "$1/extra"
}

case_args() {
  CASE_ARGS_OUT=(-N -s -C "$1" --readonly-extras -m "$1/extra")
}

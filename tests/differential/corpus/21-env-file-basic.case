CASE_DESCRIPTION="project .claude-contained/env file supplies an env var"

case_setup() {
  mkdir -p "$1/.claude-contained"
  printf 'FOO=from-file\n' > "$1/.claude-contained/env"
}

case_args() {
  CASE_ARGS_OUT=(-N -s -C "$1")
}

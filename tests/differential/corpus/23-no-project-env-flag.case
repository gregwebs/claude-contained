CASE_DESCRIPTION="--no-project-env ignores the project env file"

case_setup() {
  mkdir -p "$1/.claude-contained"
  printf 'FOO=from-file\n' > "$1/.claude-contained/env"
}

case_args() {
  CASE_ARGS_OUT=(-N -s -C "$1" --no-project-env)
}
